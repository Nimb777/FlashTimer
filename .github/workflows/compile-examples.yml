name: Compile examples

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Arduino CLI
        run: |
          # Download and extract the latest Linux x86_64 arduino-cli binary
          curl -fsSL -o /tmp/arduino-cli.tar.gz https://downloads.arduino.cc/arduino-cli/arduino-cli_latest_Linux_64bit.tar.gz
          mkdir -p /tmp/arduino-cli-extract
          tar -xzf /tmp/arduino-cli.tar.gz -C /tmp/arduino-cli-extract
          # Move the binary to /usr/local/bin and make executable. Archive layout may vary.
          if [ -f /tmp/arduino-cli-extract/arduino-cli ]; then
            sudo mv /tmp/arduino-cli-extract/arduino-cli /usr/local/bin/arduino-cli
          else
            # find binary inside extracted subfolder
            BINPATH=$(find /tmp/arduino-cli-extract -type f -name arduino-cli -print -quit)
            sudo mv "$BINPATH" /usr/local/bin/arduino-cli
          fi
          sudo chmod +x /usr/local/bin/arduino-cli
          arduino-cli version || true

      - name: Update core index (include esp cores)
        run: |
          arduino-cli core update-index --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json,https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install esp32 core
        run: arduino-cli core install esp32:esp32

      - name: Prepare ESP32 example for Arduino CLI
        run: |
          mkdir -p FlashTimer/examples/FlashTimer
          cp -f FlashTimer/examples/FlashTimer.ino FlashTimer/examples/FlashTimer/FlashTimer.ino

      - name: Install local library
        run: |
          mkdir -p "$HOME/Arduino/libraries"
          cp -R FlashTimer "$HOME/Arduino/libraries/FlashTimer"

      - name: Compile FlashTimer example for ESP32
        run: arduino-cli compile --fqbn esp32:esp32:esp32 FlashTimer/examples/FlashTimer
